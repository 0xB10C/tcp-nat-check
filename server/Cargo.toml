[package]
name = "nat-check-server"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util"] }
socket2 = { version = "0.5", features = ["all"] }

[profile.release]
strip = true
lto = true
opt-level = "z"
